/**
 *
 *
 * @author: darryl.west@roundpeg.com
 * @created: 3/22/14 7:06 AM
 */

var app = '../../app',
    services = app + '/services',
    delegates = app + '/delegates';

var AbstractApplicationFactory = require( 'node-commons' ).controllers.AbstractApplicationFactory,
    BootStrap = require( 'node-commons' ).controllers.CommonBootStrap,
    dash = require('lodash');

var ApplicationFactory = function(options) {
    'use strict';

    var factory = this;


};

module.exports = ApplicationFactory;